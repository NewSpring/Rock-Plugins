test:
  override:
    - sleep 5

deployment:
  production:
    branch: develop
    codedeploy:
      StagingIIS:
        deployment_group: Develop
        revision_location:
          revision_type: S3
          s3_location:
            bucket: ns.ops
            key_pattern: rock/develop/rock-plugins-{SHORT_COMMIT}-{BUILD_NUM}
