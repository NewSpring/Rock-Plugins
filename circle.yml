test:
  override:
    - sleep 5

deployment:
  production:
    branch: release
    codedeploy:
      StagingRock:
        deployment_group: Beta-Rock
        revision_location:
          revision_type: S3
          s3_location:
            bucket: ns.ops
            key_pattern: rock/release/rock-plugins-{SHORT_COMMIT}-{BUILD_NUM}
